#include <stdio.h>
int main()
{
	int x,y,r;
	scanf("%d",&r);
	for(x=-r;x<=r;x++)
	{   
		for(y=-r;y<=r;y++)
		{
			if(x*x*4+y*y<=r*r)
			{
				printf("*");
			}	
			else
			{
			    printf(" "); 
			}
		}   
		printf("\n");
	}
	return 0;
}
